<h1>Realizar Transferencia</h1>

<% if @error %>
  <div class="error-message">
    <%= @error %>
  </div>
<% end %>

<form action="/transfer" method="post">
  <div>
    <label for="destino">CVU o Alias destino:</label>
    <input type="text" id="destino" name="destino" required value="<%= params[:destino] %>">
  </div>

  <div>
    <label for="amount">Monto:</label>
    <input type="number" id="amount" name="amount" step="0.01" min="0.01" required value="<%= params[:amount] %>">
  </div>

  <div>
    <label for="motivo">Motivo:</label>
    <input type="text" id="motivo" name="motivo" value="<%= params[:motivo] %>">
  </div>

  <div>
    <label for="description">Descripción:</label>
    <textarea id="description" name="description"><%= params[:description] %></textarea>
  </div>

  <button type="submit">Enviar Transferencia</button>
</form>
